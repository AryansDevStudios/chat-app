
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile within the CharcoalChat application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "displayName": {
          "type": "string",
          "description": "The chosen display name for the user."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when this user profile was first created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "displayName",
        "createdAt"
      ]
    },
    "Room": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Room",
      "type": "object",
      "description": "Represents a chat room where users can exchange messages.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Room entity."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the chat room was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "createdAt"
      ]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "Represents a single message sent within a chat room, potentially including text or an image.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Message entity."
        },
        "roomId": {
          "type": "string",
          "description": "Reference to the Room where this message was sent."
        },
        "senderId": {
          "type": "string",
          "description": "Reference to the UserProfile who sent this message."
        },
        "senderDisplayName": {
          "type": "string",
          "description": "The display name of the user who sent the message."
        },
        "content": {
          "type": "string",
          "description": "The text content of the message."
        },
        "imageUrl": {
          "type": "string",
          "description": "Optional URL (or base64 string for prototype) of an attached image."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp when the message was sent.",
          "format": "date-time"
        },
        "replyToId": {
          "type": "string",
          "description": "ID of the message being replied to, if any."
        },
        "replyToContent": {
          "type": "string",
          "description": "Content of the message being replied to (denormalized)."
        },
        "replyToSenderDisplayName": {
          "type": "string",
          "description": "Display name of the sender of the replied message (denormalized)."
        }
      },
      "required": [
        "id",
        "roomId",
        "senderId",
        "senderDisplayName",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information."
        }
      },
      {
        "path": "/rooms/{roomId}",
        "definition": {
          "entityName": "Room",
          "schema": {
            "$ref": "#/backend/entities/Room"
          },
          "description": "Stores metadata for each chat room."
        }
      },
      {
        "path": "/rooms/{roomId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": {
            "$ref": "#/backend/entities/Message"
          },
          "description": "Stores individual chat messages."
        }
      }
    ]
  }
}
